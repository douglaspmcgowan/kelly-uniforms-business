title = "Project Gitleaks configuration"

[extend]
useDefault = true

[[allowlists]]
description = "Ignore local validator dependencies; authored project files are scanned separately."
paths = [
  '''^\.local/''',
  '''^\.local\\''',
  '''^\.validator-deps/''',
  '''^\.validator-deps\\''',
]
