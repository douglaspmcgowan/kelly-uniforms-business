# Current task

Goal: publish the requested temporary ordering notice through an authorized, reversible production edit.

Completed:

- Prepared the four exact Bitwarden Secrets Manager names.
- Documented the read-only machine-account pattern and current broker boundary in `ACCESS.md`.
- Preserved the requested notice, acceptance checks, and dependencies as `DEL-001`.
- Verified that the live storefront exposes OpenCart routes, Journal 3 public assets, and an active standard OpenCart admin route.
- Documented the built-in Journal Header Notice workflow, preview state, rollback, and browser checks in `WEBSITE-UPDATE-RUNBOOK.md`.

Remaining:

1. Douglas populates the website-admin Secrets Manager values and confirms the order email and phone line are ready.
2. For agent operation, build and adversarially verify the Secrets Manager-to-browser broker; an authorized human may instead use the manual login path.
3. Authenticate at the verified OpenCart admin URL and confirm the installed Journal controls and permissions.
4. Preview, publish, and verify the temporary notice across desktop and mobile.

Blocker: the website-admin Secrets Manager values, ordering-channel readiness, and agent browser broker have not been confirmed available.

Next verifier: after access preparation, verify the authenticated OpenCart identity and current Journal configuration before any production mutation.
